# 2026-09-08 工作日志

## 10:02-10:15 统一上下翻转按钮（commit 0df14f8）
- 用户实测确认功能 OK（我原计划的像素级 E2E 验证被用户确认取代，立即停止）。
- 实现：`applyGroupTransform(panel, sceneCenter)` 统一入口；全局 `allFlipped` 开关，
  rotation.x = π（默认摆正，COLMAP Y-down→Y-up）或 2π（翻转后=原始朝向），
  position 始终 = -R*sceneCenter（R 变了 t 必须重算，否则模型偏出原点）。
- 细节：flip-btn 带 active 高亮、无 ready 面板时 disabled、重置按钮还原开关；
  调试钩子 __debugSceneXform 改名 __debugGroupXform（读组级变换）。
- 险情（昨日 18:16 事后）：删除测试页时 _pages/assets 整目录 194 文件被外部进程删光
  （git 索引完好，git checkout -- 恢复零丢失）。今日 8896/8897 测试服务器已停。
- E2E 教训：page.setContent + module script + evaluate 调 window 函数会因 CSP/加载时序拿不到
  函数——纯 DOM 渲染验证别走 setContent 注入，直接用仓库里已有的独立测试页或跳过。

## 11:18 翻转逻辑简化：去掉预置 180°（commit bc2d3de）
- 用户要求：删掉 COLMAP Y-down→Y-up 的预置摆正角。默认 rotation.x=0（原始数据朝向），
  按钮纯开关：true 时 rotation.x=π。
- 居中不变：position = -R*sceneCenter，R 变 t 跟着重算（统一走 applyGroupTransform）。
- 注意：_pages/splat-compare.html 出现 data-page-node-id 属性（18 处，非本会话所加，
  疑似外部编辑器标记），暂不动它，后续可问用户是否保留。

## 11:22 清理 data-page-node-id（commit c423a06）
- 用户确认非自己生成 → 全部删掉（18 处，仅 splat-compare.html 受影响，其他页面干净）。
- 该属性是可视化编辑器类工具的 DOM 节点标记，对渲染/构建无用，纯源码污染。
- 留意：如果以后某工具又往源码里写这类属性，说明有编辑器在直接改仓库文件。
