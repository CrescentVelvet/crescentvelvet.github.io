# 2026-09-08 工作日志

## 10:02-10:15 统一上下翻转按钮（commit 0df14f8）
- 用户实测确认功能 OK（我原计划的像素级 E2E 验证被用户确认取代，立即停止）。
- 实现：`applyGroupTransform(panel, sceneCenter)` 统一入口；全局 `allFlipped` 开关，
  rotation.x = π（默认摆正，COLMAP Y-down→Y-up）或 2π（翻转后=原始朝向），
  position 始终 = -R*sceneCenter（R 变了 t 必须重算，否则模型偏出原点）。
- 细节：flip-btn 带 active 高亮、无 ready 面板时 disabled、重置按钮还原开关；
  调试钩子 __debugSceneXform 改名 __debugGroupXform（读组级变换）。
- 险情（昨日 18:16 事后）：删除测试页时 _pages/assets 整目录 194 文件被外部进程删光
  （git 索引完好，git checkout -- 恢复零丢失）。今日 8896/8897 测试服务器已停。
- E2E 教训：page.setContent + module script + evaluate 调 window 函数会因 CSP/加载时序拿不到
  函数——纯 DOM 渲染验证别走 setContent 注入，直接用仓库里已有的独立测试页或跳过。

## 11:18 翻转逻辑简化：去掉预置 180°（commit bc2d3de）
- 用户要求：删掉 COLMAP Y-down→Y-up 的预置摆正角。默认 rotation.x=0（原始数据朝向），
  按钮纯开关：true 时 rotation.x=π。
- 居中不变：position = -R*sceneCenter，R 变 t 跟着重算（统一走 applyGroupTransform）。
- 注意：_pages/splat-compare.html 出现 data-page-node-id 属性（18 处，非本会话所加，
  疑似外部编辑器标记），暂不动它，后续可问用户是否保留。

## 11:22 清理 data-page-node-id（commit c423a06）
- 用户确认非自己生成 → 全部删掉（18 处，仅 splat-compare.html 受影响，其他页面干净）。
- 该属性是可视化编辑器类工具的 DOM 节点标记，对渲染/构建无用，纯源码污染。
- 留意：如果以后某工具又往源码里写这类属性，说明有编辑器在直接改仓库文件。

## 11:33-12:10 查看模式渲染性能优化（commit 504904a）
- 用户反馈卡顿，问 PLY 渲染是否占 CPU/内存。分析结论：CPU+GPU+内存三者都占：
  1) 每帧渲染（GPU 片元）+ 每帧 controls.update；
  2) 相机一动就触发 CPU 排序（WASM worker，百万 splat 毫秒级，角度阈值内还有部分排序）；
  3) 常驻内存 ≈ 文件大小 ×2-3（解析中间态 + 纹理），297MB PLY ≈ 600-900MB。
- **主因**：我们的渲染循环每帧无条件全量渲染（旧代码 rAF 永不挂起）。
- 修复（_pages/splat-compare.html）：
  - 按需渲染：静止 20 帧 + 无 viewer.sortRunning 在跑 → 挂起 rAF；
    唤醒点=controls change/setAllFlipped/relayout 尺寸变化/加载完成。
    （fitAllToFirst 走 controls.update() 自动触发 change，无需额外唤醒）
  - setPixelRatio(1)（原 dpr，HiDPI 下片元 4 倍）；antialias:false（MSAA 对 splat 无效）。
  - 显式 sphericalHarmonicsDegree:0（确认 v0.4.7 默认即 0，仅防回归）。
- **库源码结论**（v0.4.7）：
  - renderMode:OnChange 只在自驱动模式经 shouldRender() 生效，DropIn 模式无效，别传。
  - sortWorker 完成回调里 forceRenderNextFrame 只置库内部标志，DropIn 模式靠外层继续画帧。
  - 挂起前必须检查 viewer.sortRunning（否则挂在旧排序结果上）。
  - OrbitControls.update() 返回"是否变化"bool（position/quat/target 距离阈值），可作动量检测。
- **E2E 大文件注入经验**：297MB PLY 注入页面：Array.from(bytes) 超数组长度上限 →
  RangeError；base64 传参 400MB 字符串 → 页面崩溃。正确姿势：Node 脚本内嵌
  http.createServer listen(0) 随机端口 + fetch。注意 CJS 顶层不能有 await（包 IIFE）。
- **合成 PLY 无头渲染问题**：synth.ply 在 swiftshader 无头环境渲染不出（真实.ply 正常），
  此前多次截图对比的"红蓝像素"其实是工具栏文字的 ClearType 次像素伪影，不是 splat！
  像素断言必须先验"内容像素量"（non-bg 占比），别拿纯背景图对比出假 PASS/FAIL。

## 17:35-17:55 沙盘：导弹车发射与飞行表现重做（commit 55e0c41）
文件 `_pages/sandbox_war_game.html`。峰哥要求：弹道导弹像火箭（喷火飞出 + 末尾加速砸中）、
巡航导弹像无人机（缓慢飞出 + 跟踪一段后爆炸）、车上剩余弹数一眼可见。

### 弹道导弹（BallisticMissile）
- 造型重做：红尖锥弹头 + 白弹体 + 红环带 + 尾翼（clip-path），
  常态喷焰 5×10 带 `missileFlame` 抖动；`.boosting` 时喷焰拉到 7×22 并加发光。
- **末端加速**：新增 `boostAt`/`boostFactor`（0.55 / 2.8）。Projectile.update 里按
  行程比例 t 判定 `this.boosting`，`effSpeed = base * speedMul`。实测 2.22s vs 无加速 3.07s，
  峰值 560px/s。加速判定与抛物线切线角度计算共用同一个 t（已提取 bt 避免重复算）。
- 尾迹：`emitTrail()` 每 0.05s 在弹尾生成 `.smoke-puff`（0.6s 淡出放大），
  加速时尾焰长 → 烟团起点后移 18 单位。

### 巡航导弹（CruiseMissile）
- **真正的跟踪**：原来只飞向发射时算好的固定点，目标一动就打空。新增 `proj.homing`，
  末端航路点每帧把 scatterX/Y 更新为 `target.x + getSize()/2`（目标死亡则停更）。
  仿真验证目标 20px/s 逃跑仍能在 3.77s 追上，不会无限追击。
- 造型：无人机化——银白机身 + 大后掠主翼(::before) + 尾翼(.drone-tail) +
  阵营色导引头(::after) + 闪烁航行灯(.drone-beacon)。尾焰**巡航段就常显**（小），
  `.diving` 时拉长到 15px。
- 末端锁定圈 `.drone-lock`：红圈咬住目标脉动，进入末端才出现，`remove()` 里清理。
  由 `updateLock(onTerminal)` 每帧定位。
- 速度：巡航 130（原 150，更慢更有来袭感）/ 末端 420。

### 载弹显示（17:47 峰哥追加要求后定稿，commit e334cf3）
- **不要空筒**：峰哥明确"导弹直接放在车上，打完了就没了"。最终方案是甲板上
  **只渲染剩余弹药数**的导弹（`.ballistic-missile` / `.cruise-missile`），
  打一发少一根，`ammo=0` 时甲板空空如也。旧的 `.ballistic-tube` / `.cruise-cell`
  （含 .loaded 切换与空筒黑洞）全部删除。
- 甲板容器 `.ballistic-rack/.cruise-rack`：`left:3 width:30 top:7 height:14`，
  flex `justify-content:center` + `align-items:flex-end`——**剩余导弹自动居中**，
  打空后不会偏向一侧；弹底坐在 chassis 甲板 y=21 上。
- 尺寸校验：导弹 4px 宽，弹道 gap:4（3 发 = 20px）、巡航 gap:2（5 发 = 28px），
  均 ≤ 30px 甲板宽。巡航主翼收到 6px，避免 pitch 6px 下相邻弹翼重叠。
- 刷新逻辑 `buildMissileRack()`：markup 生成与 updateVisual 共用；
  updateVisual 里用 `rack.childElementCount !== ammo` 做守卫，数量变化才重建 innerHTML。

### 复用要点
- 弹丸 visual 由 `specs[source.type]` 决定，**构造函数里就已知**，所以 drone 的
  尾翼/尾焰/航行灯在构造函数里 `innerHTML` 一次性注入，不要再在 tryAttack 里 append。
- 弹体渲染中心 ≠ 逻辑坐标（抛物线弧高 / 巡航高度都是视觉抬升），
  `updateVisual()` 里记 `renderX/renderY/dirX/dirY`，尾迹定位必须用它们。
- 无头仿真 harness（`.workbuddy/tmp/missile_sim.js`）：截取 `class Projectile`→`class Obstacle`
  源码 + document/battlefieldManager stub，可跑完整弹道。stub 必须提供
  `appendChild` **和** `insertBefore`（updateShadow 用 insertBefore 插地面阴影）。
