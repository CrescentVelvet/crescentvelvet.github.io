# 2026-09-07 工作日志

## 高斯模型对比页面（_pages/splat-compare.html）

### 背景
峰哥要在 GitHub Pages 站点加一个高斯泼溅 PLY 模型对比查看器，用于消融实验对比多个模型。

### 技术选型（经 grill-me 技能多轮决策确定）
- **渲染库**：three.js + GaussianSplats3D（`@mkkellogg/gaussian-splats-3d` v0.4.7）
- **加载方式**：纯 `<script type="module">` + import map，库文件下载到 `assets/js/vendor/` 本地引用
- **不用 supersplat-viewer**：它设计为单实例，多窗口相机联动要 hack，不如 three.js 原生多 renderer 方便

### 实现要点
- **多窗口布局**：CSS Grid，2→1×2 / 3→1×3 / 4→2×2 自适应，固定等分，细 gap + 暗色背景
- **相机联动**：完全联动，同步轨道角(azimuth/elevation) + 相对距离比例 + target；每个模型按包围盒归一化到统一虚拟尺寸(4单位)；用 OrbitControls 的 change 事件广播，syncLock 防循环
- **GaussianSplats3D 用法**：`selfDrivenMode:false` + 自定义 renderer/camera/`useBuiltInControls:false`，自己跑渲染循环；OrbitControls 从库直接 export 使用
- **交互**：整页拖拽 + 文件选择按钮，每窗口独立 loading spinner+进度条，标题栏可编辑+关闭按钮，首个加载完自动 fit all

### 文件
- `_pages/splat-compare.html`（新建，permalink: /splat-compare/，layout: null）
- `assets/js/vendor/three.module.js`（three v0.185.1，650KB）
- `assets/js/vendor/gaussian-splats-3d.module.js`（GS3D v0.4.7，617KB）
- `_pages/about.md`（主页导航加入"高斯对比"按钮）

### 重要坑：assets/js/vendor 被 _config.yml exclude
minimal-mistakes 主题的 `_config.yml` 第 141 行 `exclude:` 列表里有 `assets/js/vendor`，
导致放这个目录的 JS 文件不会被 Jekyll 复制到 _site，浏览器请求返回 text/html（404页），
表现为 module 脚本加载失败、事件监听不注册、拖拽文件直接被浏览器打开。
**解决方案**：库文件改放 `assets/splat-vendor/`（不在 exclude 列表），正常作为静态资源服务。

### 重要坑：GaussianSplats3D v0.4.7 真实 API（非臆测）
- `viewer.getSplatMesh()` ✅ 存在，返回 SplatMesh（继承自 THREE.Mesh）
- `splatMesh.getScene(0)` ✅ 存在，返回 SplatScene（继承自 THREE.Object3D）
- `scene.splatBuffer` ✅ 有 `sceneCenter` 属性（THREE.Vector3）
- `scene.position` / `scene.scale` / `scene.quaternion` ✅ 直接设即可做 transform
- ❌ `splatMesh.getBoundingBox()` 不存在
- ❌ `splatMesh.setScaleAndOffset()` 不存在
- 归一化方案：加载后 `scene0.position.copy(sceneCenter).multiplyScalar(-1)` 把中心移到原点，
  相机 target 统一用 (0,0,0)，联动只同步球面坐标 azimuth/elevation/distance
- Viewer 无自动 fit 方法，初始视角需手动设（距离3/俯角60°/方位45°）

### 待验证
- 真实 297MB PLY 在浏览器加载耗时与渲染效果
- 相机联动在多模型场景下的实际效果

### 重要坑：three.js v0.185.1 模块拆分
`three.module.js` 内部用相对路径 `import {...} from './three.core.js'` 拆成两个文件，
只下载入口 `three.module.js` 不够，必须同时下载 `three.core.js`，否则浏览器报 404。
unpkg 路径：`https://unpkg.com/three@0.185.1/build/three.core.js`（1.4MB）。
排查方法：看 three.module.js 第 6-7 行的相对 import 语句。

### 重要坑：blob URL 无法被 sceneFormatFromPath 识别
GaussianSplats3D 的 `addSplatScene` 默认用 `sceneFormatFromPath(path)` 从路径后缀推断格式，
blob URL（`blob:http://...`）无扩展名 → 推断返回 null → 报 "File format not supported"。
**解决方案**：`addSplatScene` 的 options 里显式传 `format: SceneFormat.Ply`（值为 2），
并从库 import `SceneFormat`。源码位置：gaussian-splats-3d.module.js 第 12968 行。

### 重要坑：SharedArrayBuffer 需要 crossOriginIsolated
GaussianSplats3D 默认 `sharedMemoryForWorkers: true`，用 SharedArrayBuffer 在主线程和
sort Worker 间通信，要求页面 `self.crossOriginIsolated === true`，需要服务器发两个头：
- `Cross-Origin-Opener-Policy: same-origin`
- `Cross-Origin-Embedder-Policy: require-corp`
Jekyll/GitHub Pages 默认不发这俩头 → `postMessage` 传 SharedArrayBuffer 抛 DataCloneError。
**解决方案**：Viewer 构造时传 `sharedMemoryForWorkers: false`，改用普通 transferable，
性能略降但无需特殊 HTTP 头。源码位置：gaussian-splats-3d.module.js 第 12316 行。
注意：GitHub Pages 生产环境也不发 COOP/COEP 头，所以必须用 false。

## 16:52-17:40 黑屏根因定案与修复
- 矩阵探针（7 变体）锁定根因：`gpuAcceleratedSort: true` 必黑屏，其余变量（halfPrecision、sceneCenter 偏移、手动 update、forceSortAll）均无辜。
- 深度诊断：GPU precomputedDistances 数据正常（115 万非零），但 worker 回信 splatRenderCount=0。
- 根因链：库 L13338 开 GPU 排序时不下发 centers → worker `uploadedSplatCount=0` → L11580 clamp → instanceCount=0。库 v0.4.7 bug，官方组合踩不到。
- 修复：splat-compare.html L329 改 `gpuAcceleratedSort: false` + 详细注释。
- E2E 验证：DataTransfer 模拟拖放 real.ply（297MB/115 万 splat）→ nonBg 24.7%、1652 色 → 修复确认。
- 顺带发现：real.ply 的 PLY 头没有 sceneCenter 字段（解析为 0,0,0），此前"sceneCenter 偏移"假设失效；但偏移与否矩阵测试均正常渲染。

## 17:38 翻转修复 + 平移联动
- Y 翻转：3DGS 数据是 COLMAP Y-down 约定，three.js Y-up 下颠倒。
- 平移联动：原来只同步球面坐标且强制 target=原点 → 右键平移（改 target）被丢弃。修：camera state 带 target 一起广播/应用（computeCameraState/applyCameraState 全量同步）。E2E 探针读 __debugCameras 验证 PASS（两面板 target 同步 0.000000）。
- 合成 PLY opacity bug 修正：应存 logit(0.99)≈4.6（库端做 sigmoid）。

## 17:50 关键发现：scene0 变换在 static 模式下被烘焙，事后改无效
- 现象：改 scene0.rotation/position 后截图像素 diff 为 0（mean=0.0），完全不生效。
- 根因（库源码定位）：v0.4.7 `dynamicScene=false` 时 `fillSplatDataArrays` 里 `applySceneTransform = !dynamicMode = true`，把 scene 变换烘焙进数据纹理（centers/covariance/SH），build 之后 scene0 的 Object3D 属性不再参与渲染。`SplatScene.updateTransform` 非动态模式只拷 `this.matrix`（局部），不吃 matrixWorld。
- 正确做法：变换应用到 **splatGroup（THREE.Group）级**——splatMesh.matrixWorld 含 group 变换，排序（mvpMatrix × splatMesh.matrixWorld）和渲染（场景图）都吃这套。
- 修复代码（splat-compare.html L391）：`splatGroup.rotation.set(π,0,0)` + `splatGroup.position = -(R*sceneCenter)`（applyEuler 后取负）。
- 验证：修复后 numpy 像素 diff mean=56.65、39 万像素变化（对比修复前 0）；`__debugSceneXform` 确认 scene0 未动、group 承载旋转。绝对朝向（正立/倒立）像素相关性测试不确定（透视+模型对称），待用户浏览器目视确认。
- E2E 经验：Playwright 模拟鼠标拖拽 OrbitControls 不可靠（右键平移 |dL|=0），直接读 window.__debugCameras() 状态断言更稳。

## 18:15 清理测试页 + 一次险情处理
- 用户要求删除测试页：git rm 了 5 个 splat-test 探针页 + assets/splat-test/（synth.ply、three160.module.js 5.3 万行），commit 78087fa。均无引用（grep 过产品页/about/_includes/_layouts）。
- **险情**：删除时 git status 显示 194 个已跟踪文件未暂存删除（_pages、assets 整目录从磁盘消失）——非本次 git rm 所为（它只暂存 7 个文件），也非索引损坏（HEAD 370 = 索引 363 + 7），是外部原因（疑似杀毒/同步盘/Jekyll watch 异常）。用 `git checkout -- _pages assets` 从索引完整恢复，零丢失。
- 教训：git rm 前后如果 status 输出与预期不符（量级不对），必须先停下来查清（ls-tree vs ls-files vs 磁盘三对账），绝不顺手 reset/commit。
- e2e2_out.txt 临时输出已删；_site 里无残留 test 页。
